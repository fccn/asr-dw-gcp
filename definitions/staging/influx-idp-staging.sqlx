config {
  type: "view",
  name: "influx-idp-staging",
  description: "Extract IDP data from InfluxDB logs.",
  tags: ["rctsaai"]
}
SELECT DISTINCT 
  idp_id AS idp,
  'unknown' AS status
FROM ${ref("fetched-influx-logs")}
WHERE idp_id IS NOT NULL