config {
  type: "incremental",
  bigquery: {
    partitionBy: "full_date" 
  }
}

INSERT INTO
  ${ref("fact_distinct_user_hit")}
SELECT
  *
FROM
  ${ref("distinct_user_hit")}
WHERE
  TRUE
